export const THEME_NAMES = {
  dark: 'dark',
  light: 'light',
  custom: 'custom',
};

export const THEME_TYPE_KEY = 'theme_type';

export const COLOR_SCHEME_THEME_MAP = {
  'no-preference': THEME_NAMES.light,
  dark: THEME_NAMES.dark,
  light: THEME_NAMES.light,
};
